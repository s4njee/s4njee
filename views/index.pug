extends layout

block header
    include header
block content
  h1= title
  each post in posts
      div(class="container-fluid")
          div(class="col-md-6 col-md-offset-3 postdiv")
              p(class="date")= post.date 
              p(class="time")= post.time
              p(class="post")!= post.post
